datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["nativeTypes"]
}

model StickerPack {
  chatId BigInt @id     @db.BigInt
  userId BigInt         @db.BigInt
  name   String @unique
}
